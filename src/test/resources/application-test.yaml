spring:
  datasource:
    username: sa
    password: pwd
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL
    driverClassName: org.h2.Driver

logging:
  level:
    root: info
    com:
      valentyn: debug